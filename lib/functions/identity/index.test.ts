import { /* expect, */ test } from "vitest"

// import * as fc from "fast-check"

// import identity from "."

// FIXME
test("[identity] (functions) returns the identical item passed", () => {
	// fc.assert(fc.property(fc.anything(), x => expect(identity(x)).toBe(x)))
})
